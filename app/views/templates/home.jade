.home-page()
  .container
    .header
      h1 Code crush and critic App
      hr
      h1.lead This should be converted to CCAC
    .todo-form
      form
        .form-group
          input.form-control.input-lg(type='text', placeholder='Enter text...', ng-model='formData.text')
        button.btn.btn-primary.btn-lg.btn-block(type='submit', ng-click='createTodo()') Add Todo
    br
    .todo-list
      ul(ng-repeat='todo in todoData track by $index')
        li
          h3
            input.lead(type='checkbox', ng-click='deleteTodo(todo.id)')
            |  {{ todo.text }}
        hr

