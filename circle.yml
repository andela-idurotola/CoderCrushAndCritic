deployment:
  staging:
    branch: master
machine:
  node:
    version:
      v0.10.33
test:
  pre:
    - npm start:
        background: true
    - bower install
  override:
    - npm run jade
    - npm run test